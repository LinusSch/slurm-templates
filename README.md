# Slurm example batch scripts

A few Slurm batch scripts intended to serve as examples, cheatsheets and
recommended templates.

This original repo is (for now) demonstrating the UPPMAX cluster Pelle, which is
a pretty basic Slurm install, therefore should be a reasonable starting point
for adapting to other clusters.
